# 파이썬 가상환경 — 새 위치/새 머신에서 매번 다시 만들 것 (requirements.txt/pyproject 참고)
.venv/
__pycache__/
*.pyc
*.pyo

# 파이프라인 산출물 — 코드로 다시 만들 수 있는 중간/결과 파일 (프레임, 마스크, sparse 복원, 메쉬 등)
data/output/

# 자동 다운로드되는 모델 가중치 — masking.load_skin_segmenter() 등이 없으면 알아서 받아온다
data/models/

# 에디터/OS 잡파일
.DS_Store
Thumbs.db
